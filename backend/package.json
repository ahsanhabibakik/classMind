{
  "scripts": {
    "start": "uvicorn app.main:app --reload",
    "start:prod": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest tests/ -v",
    "lint": "pylint app/",
    "format": "black app/",
    "type-check": "mypy app/"
  }
}
